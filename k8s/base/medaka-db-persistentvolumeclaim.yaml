apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: medaka-db
  name: medaka-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
